- name: play level variables
  hosts: local
  connection: local
  vars:
   course: "devops with aws"
   duration: "120hrs"
   trainer: "asif ali "
  tasks:
  - name: print variables
    vars:
     COURSE: ansible
    ansible.builtin.debug:
      msg: "course is: {{course}}, {{trainer}}"